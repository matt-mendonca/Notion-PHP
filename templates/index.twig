<!DOCTYPE html>
{% include 'header.twig' %}

    <body>
        <div class="row">
            <div class="large-12 columns">
                <h1>{{ content_title }}</h1>
                <div class="content">
                    {{ content | raw }}
                </div>
            </div>
        </div>

        {% include 'footer.twig' %}
    </body>
</html>